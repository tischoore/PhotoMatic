{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug PhotoMatic",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/target/debug/photomatic.exe",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "cargo build"
        }
    ]
}
